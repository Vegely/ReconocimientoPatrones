# ReconocimientoPatrones

## Data Setup (Windows)

The dataset is hosted externally to reduce repository size. To download and configure the data:

**Option 1: Double-Click**
Simply double-click the `setup.bat` file in the main folder.

**Option 2: Manual (PowerShell)**
1. `pip install gdown`
2. `python download_data.py`